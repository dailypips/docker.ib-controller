#if you run multiple ib gateways, you have to set a unique logging directory for each instance.

IBC_NAME=
IB_PORT=

IBC_CONFIG=$(pwd)/configs/${IBC_NAME}.ini
IB_CONFIG_DIR=$HOME/Jts/${IBC_NAME}

IBC_LOG=$HOME/log/ib-controller-$IBC_NAME

SSH_PORT=1${IB_PORT}